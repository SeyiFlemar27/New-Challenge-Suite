"use client";
import Link from "next/link";import {ArrowRight,Search} from "lucide-react";import {useEffect,useState} from "react";import {PublicFooter,PublicHeader} from "@/components/public-site/public-shell";import type {PublicCategory} from "@/lib/public-site/config";
type Challenge={id:string;title:string;shortDescription:string;coverImageUrl:string;category:string;detailHref:string;participantCount:number;creator:{displayName:string};cta:{label:string;href:string;disabled?:boolean};phaseSummary:{label:string}};
export function CategoryExperience({category}:{category:Omit<PublicCategory,"icon">}){const [specialty,setSpecialty]=useState(""),[search,setSearch]=useState(""),[page,setPage]=useState(1),[items,setItems]=useState<Challenge[]>([]),[total,setTotal]=useState(0),[loading,setLoading]=useState(true),[more,setMore]=useState(false);useEffect(()=>{setLoading(true);const query=new URLSearchParams({category:category.name,page:String(page),limit:"12",sort:"recent"});if(search||specialty)query.set("q",search||specialty);fetch("/api/explore/challenges?"+query).then(r=>r.json()).then(x=>{const rows=x.data?.challenges||[];setItems(current=>page===1?rows:[...current,...rows]);setTotal(Number(x.data?.total||0));setMore(Boolean(x.data?.hasMore))}).finally(()=>setLoading(false))},[category.name,page,search,specialty]);function filter(value:string){setSpecialty(value);setPage(1)}
return <main className="public-page"><PublicHeader/><section className="public-section bg-[#f5f5f1]"><div className="public-container"><p className="text-sm font-bold text-[#8b7900]">Challenge category</p><h1 className="mt-4 max-w-4xl text-[clamp(2.6rem,6vw,4.8rem)] font-semibold leading-tight">{category.name} challenges</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-[#5f5f5f]">{category.description}</p><form className="mt-8 flex max-w-xl rounded-[10px] border border-[#d7d7d2] bg-white p-2" onSubmit={e=>{e.preventDefault();setPage(1)}}><Search className="ml-2 self-center text-[#777]" size={19}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder={"Search "+category.name.toLowerCase()+" challenges"} className="min-w-0 flex-1 bg-transparent px-3 outline-none"/><button className="public-primary-button">Search</button></form></div></section><section className="public-section"><div className="public-container"><div className="flex flex-wrap gap-2" aria-label="Specialty filters"><button onClick={()=>filter("")} className={!specialty?"public-pill-active":"public-pill"}>All</button>{category.specialties.map(x=><button key={x} onClick={()=>filter(x)} className={specialty===x?"public-pill-active":"public-pill"}>{x}</button>)}</div><div className="mt-10 flex items-end justify-between"><div><h2 className="text-3xl font-semibold">Relevant challenges</h2><p className="mt-2 text-[#666]">{total} public result{total===1?"":"s"}</p></div><Link href={"/explore?category="+encodeURIComponent(category.name)} className="text-sm font-bold underline">Open full search</Link></div>{loading&&page===1?<div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{[1,2,3,4,5,6].map(x=><div key={x} className="h-80 animate-pulse rounded-[12px] bg-[#f2f2ee]"/>)}</div>:items.length?<div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{items.map(item=><article key={item.id} className="overflow-hidden rounded-[12px] border border-[#deded8] bg-white"><Link href={item.detailHref} className="block aspect-[16/9] bg-[#ededeb]">{item.coverImageUrl?<img src={item.coverImageUrl} alt="" className="h-full w-full object-cover"/>:<span className="flex h-full items-center justify-center text-sm text-[#777]">Challenge media not available</span>}</Link><div className="p-5"><p className="text-xs font-bold text-[#8b7900]">{item.phaseSummary?.label||item.category}</p><h3 className="mt-2 text-xl font-semibold">{item.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-[#666]">{item.shortDescription}</p><p className="mt-4 text-xs text-[#777]">By {item.creator?.displayName} · {item.participantCount||0} participants</p><Link href={item.cta?.href||item.detailHref} aria-disabled={item.cta?.disabled} className="public-secondary-button mt-5 w-full justify-center">{item.cta?.label||"View challenge"}<ArrowRight size={16}/></Link></div></article>)}</div>:<div className="mt-8 rounded-[12px] border border-[#deded8] bg-[#fafaf8] p-10 text-center"><h3 className="text-xl font-semibold">Oops, no challenges yet</h3><p className="mt-2 text-[#666]">Try again later.</p></div>}{more?<button onClick={()=>setPage(v=>v+1)} disabled={loading} className="public-secondary-button mx-auto mt-8">{loading?"Loading...":"Load more"}</button>:null}</div></section><section className="public-section bg-[#f5f5f1]"><div className="public-container grid gap-8 lg:grid-cols-2"><div><p className="text-sm font-bold text-[#8b7900]">How it works</p><h2 className="mt-4 text-4xl font-semibold">Compete in {category.name.toLowerCase()}.</h2><p className="mt-4 leading-7 text-[#626262]">Review each challenge's timeline and requirements, join through its official entry flow, then submit before the deadline.</p></div><div className="rounded-[14px] bg-[#f5d90a] p-8"><h3 className="text-2xl font-semibold">Ready to find your next challenge?</h3><Link href={"/explore?category="+encodeURIComponent(category.name)} className="mt-6 inline-flex items-center gap-2 font-bold">Explore {category.name}<ArrowRight size={18}/></Link></div></div></section><PublicFooter/></main>}
