"use client";
import Link from "next/link";
import {ArrowRight,Check,ChevronRight,CircleDollarSign,Search,ShieldCheck,Sparkles,Trophy,UsersRound} from "lucide-react";
import {useEffect,useMemo,useRef,useState} from "react";
import {useAuth} from "@/components/auth-provider";
import {PublicFooter,PublicHeader} from "@/components/public-site/public-shell";
import {calculateChallengeEconomics,type CalculatorInput} from "@/lib/public-site/economics";
import {calculatorCreationHref,DEFAULT_PUBLIC_CONTENT,protectedPublicHref,PUBLIC_CATEGORIES} from "@/lib/public-site/config";
import {trackPublicEvent} from "@/lib/public-site/analytics";
type Data={content:typeof DEFAULT_PUBLIC_CONTENT;announcement:typeof DEFAULT_PUBLIC_CONTENT.announcement|null;stats:Array<{label:string;value:number;prefix:string;suffix:string}>;stories:Array<{id:string;quote:string;displayName:string;role:string;portraitUrl:string}>};
type Plan={id:string;name:string;priceMonthly:number|null;features:string[];current?:boolean};
type Suggestion={id:string;title:string;category:string;detailHref:string};
const talentSteps=[["Discover a challenge","Browse opportunities by category, prize, format, deadline or entry requirements."],["Join and submit","Register, complete any required payment and upload your entry through the guided submission flow."],["Compete and get recognised","Follow voting, rankings and results, then receive rewards when you win."]];
const sponsorSteps=[["Discover opportunities","Find challenges, creators and audiences that match your campaign goals."],["Propose or confirm sponsorship","Review the opportunity, define your contribution and agree on deliverables."],["Track the campaign","Follow participation, visibility, engagement and final campaign results."]];
export function PublicHome(){const auth=useAuth(),[data,setData]=useState<Data>({content:DEFAULT_PUBLIC_CONTENT,announcement:DEFAULT_PUBLIC_CONTENT.announcement,stats:[],stories:[]}),[plans,setPlans]=useState<Plan[]>([]);useEffect(()=>{let active=true;Promise.all([fetch("/api/public/home").then(r=>r.json()),fetch("/api/subscriptions").then(r=>r.json())]).then(([home,sub])=>{if(!active)return;if(home.ok)setData(home.data);if(sub.ok)setPlans((sub.data.plans||[]).filter((p:Plan)=>p.id==="creator"||p.id==="host"))}).catch(()=>undefined);return()=>{active=false}},[]);const profile=(auth.profile??{}) as Record<string,unknown>;return <main className="public-page"><PublicHeader announcement={data.announcement}/><Hero content={data.content.hero} authenticated={!!auth.user}/><Metrics items={data.stats}/><Categories/><How/><Calculator authenticated={!!auth.user}/><Pricing plans={plans} authenticated={!!auth.user}/><Stories items={data.stories}/><Assurances items={data.content.assurances}/><Final content={data.content.finalCta} authenticated={!!auth.user} sponsor={String(profile.accountType||profile.role||"")==="sponsor"}/><PublicFooter/></main>}
function Hero({content,authenticated}:{content:typeof DEFAULT_PUBLIC_CONTENT.hero;authenticated:boolean}){const [mode,setMode]=useState<"talent"|"sponsor">("talent"),[q,setQ]=useState(""),[items,setItems]=useState<Suggestion[]>([]),[open,setOpen]=useState(false),[active,setActive]=useState(-1);useEffect(()=>{if(localStorage.getItem("challenge_suite_public_role")==="sponsor")setMode("sponsor")},[]);useEffect(()=>{if(q.trim().length<2){setItems([]);return}const timer=setTimeout(()=>fetch("/api/explore/challenges?q="+encodeURIComponent(q.trim())+"&limit=8").then(r=>r.json()).then(x=>{setItems((x.data?.challenges||[]).map((v:Record<string,unknown>)=>({id:String(v.id),title:String(v.title),category:String(v.category),detailHref:String(v.detailHref)})));setOpen(true);setActive(-1)}).catch(()=>setItems([])),180);return()=>clearTimeout(timer)},[q]);const chips=mode==="talent"?["Singing","Fashion & Modelling","Fitness","Photography","Gaming"]:["Trending opportunities","Sports","Creative campaigns","Live events","Community challenges"];const go=()=>{if(!q.trim())return;localStorage.setItem("challenge_suite_recent_search",q.trim());trackPublicEvent("public_search",{mode,query_length:q.trim().length});location.href="/explore?q="+encodeURIComponent(q.trim())+(mode==="sponsor"?"&sponsorReady=true":"")};const keys=(e:React.KeyboardEvent<HTMLInputElement>)=>{if(e.key==="ArrowDown"){e.preventDefault();setActive(v=>Math.min(v+1,items.length-1))}else if(e.key==="ArrowUp"){e.preventDefault();setActive(v=>Math.max(0,v-1))}else if(e.key==="Escape")setOpen(false);else if(e.key==="Enter"){e.preventDefault();active>=0&&items[active]?location.href=items[active].detailHref:go()}};
return <section className="public-container pb-16 pt-5 sm:pb-24"><div className="public-hero"><video className="absolute inset-0 h-full w-full object-cover" autoPlay muted loop playsInline preload="metadata" poster={content.posterUrl} aria-label="People collaborating and competing"><source src={content.videoUrl} type="video/mp4"/>Your browser does not support background video.</video><div className="absolute inset-0 bg-[linear-gradient(90deg,rgba(8,8,8,.92)_0%,rgba(8,8,8,.72)_52%,rgba(8,8,8,.25)_100%)]"/><div className="relative flex min-h-[650px] items-end px-5 py-9 sm:px-10 sm:py-12 lg:min-h-[690px] lg:items-center lg:px-16"><div className="max-w-[720px]"><p className="text-sm font-bold text-[#f8df35]">{content.eyebrow}</p><h1 className="mt-5 whitespace-pre-line text-[clamp(2.55rem,6vw,4.75rem)] font-semibold leading-[1.03] text-white">{content.headline}</h1><p className="mt-6 max-w-xl text-base leading-7 text-white/80 sm:text-lg">{content.supportingCopy}</p><div className="mt-7 inline-flex rounded-full bg-white/12 p-1" role="tablist"><button role="tab" aria-selected={mode==="talent"} onClick={()=>{setMode("talent");localStorage.setItem("challenge_suite_public_role","talent");trackPublicEvent("hero_mode_changed",{mode:"talent"})}} className={mode==="talent"?"public-role-active":"public-role"}>I'm a Talent</button><button role="tab" aria-selected={mode==="sponsor"} onClick={()=>{setMode("sponsor");localStorage.setItem("challenge_suite_public_role","sponsor");trackPublicEvent("hero_mode_changed",{mode:"sponsor"})}} className={mode==="sponsor"?"public-role-active":"public-role"}>I'm a Sponsor</button></div><div className="relative mt-5 max-w-2xl"><div className="flex min-h-16 items-center rounded-[12px] bg-white p-2 shadow-xl"><Search className="ml-3 text-[#676767]" size={20}/><input role="combobox" aria-autocomplete="list" aria-expanded={open&&q.length>=2} aria-controls="hero-search-list" aria-activedescendant={active>=0?"hero-result-"+active:undefined} value={q} onChange={e=>setQ(e.target.value)} onFocus={()=>setOpen(true)} onKeyDown={keys} placeholder={mode==="talent"?"Search challenges, categories or opportunities":"Search challenges, creators or sponsorship opportunities"} className="min-w-0 flex-1 bg-transparent px-3 text-[15px] text-[#171717] outline-none"/><button type="button" onClick={go} className="public-primary-button h-12 px-5">Search</button></div>{open&&q.trim().length>=2?<div id="hero-search-list" role="listbox" className="absolute inset-x-0 top-[calc(100%+8px)] z-20 rounded-[12px] border border-[#deded8] bg-white p-2 text-[#171717] shadow-xl">{items.length?items.map((item,index)=><Link id={"hero-result-"+index} role="option" aria-selected={active===index} key={item.id} href={item.detailHref} className={"flex min-h-12 items-center justify-between rounded-[8px] px-3 text-sm "+(active===index?"bg-[#fff7ba]":"hover:bg-[#f5f5f1]")}><span><b>{item.title}</b><small className="ml-2 text-[#777]">{item.category}</small></span><ChevronRight size={16}/></Link>):<p className="px-3 py-4 text-sm"><b>Oops, no challenges yet</b><br/><span className="text-[#777]">Try again later.</span></p>}</div>:null}</div><div className="mt-4 flex flex-wrap gap-2">{chips.map(chip=><button key={chip} onClick={()=>{setQ(chip);setOpen(true)}} className="rounded-full border border-white/25 px-3 py-2 text-xs font-bold text-white hover:border-[#f8df35]">{chip}</button>)}</div><div className="mt-6 flex gap-5 text-sm text-white/75"><Link href={protectedPublicHref("/challenges/create",authenticated)} className="underline underline-offset-4">Create a challenge</Link><Link href={protectedPublicHref("/host/live/create",authenticated)} className="underline underline-offset-4">Host an event</Link></div></div></div></div></section>}
function Metrics({items}:{items:Data["stats"]}){const ref=useRef<HTMLDivElement>(null),[run,setRun]=useState(0);useEffect(()=>{if(!ref.current)return;const o=new IntersectionObserver(e=>{if(e[0]?.isIntersecting)setRun(v=>v+1)},{threshold:.45});o.observe(ref.current);return()=>o.disconnect()},[]);if(!items.length)return null;return <section className="public-section pt-0"><div ref={ref} className="public-container grid border-y border-[#deded8] sm:grid-cols-2 lg:grid-cols-4">{items.slice(0,4).map(item=><Metric key={item.label} item={item} run={run}/>)}</div></section>}
function Metric({item,run}:{item:Data["stats"][number];run:number}){const [n,setN]=useState(0);useEffect(()=>{if(!run)return;if(matchMedia("(prefers-reduced-motion: reduce)").matches){setN(item.value);return}let start=0,id=0;const tick=(t:number)=>{if(!start)start=t;const p=Math.min(1,(t-start)/650);setN(Math.round(item.value*(1-Math.pow(1-p,3))));if(p<1)id=requestAnimationFrame(tick)};id=requestAnimationFrame(tick);return()=>cancelAnimationFrame(id)},[run,item.value]);return <div className="border-[#deded8] px-5 py-8 text-center sm:border-r last:border-r-0"><p className="text-3xl font-semibold">{item.prefix}{n.toLocaleString()}{item.suffix}</p><p className="mt-2 text-sm text-[#696969]">{item.label}</p></div>}
function Categories(){return <section id="categories" className="public-section"><div className="public-container"><Heading eyebrow="Browse by category" title="Find the kind of challenge that fits you." body="Explore public competitions across creative, technical, professional and community categories."/><div className="mt-10 grid grid-cols-2 gap-3 md:grid-cols-3 lg:grid-cols-5">{PUBLIC_CATEGORIES.map(({slug,name,icon:Icon})=><Link key={slug} href={"/categories/"+slug} onClick={()=>trackPublicEvent("category_opened",{category:slug})} className="public-category-card"><Icon size={21}/><span>{name}</span><ArrowRight size={16} className="ml-auto opacity-0 group-hover:opacity-100"/></Link>)}</div></div></section>}
function How(){const [mode,setMode]=useState<"talent"|"sponsor">("talent"),steps=mode==="talent"?talentSteps:sponsorSteps;return <section className="public-section bg-[#f5f5f1]" id="how-it-works"><div className="public-container"><div className="flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between"><Heading eyebrow="How it works" title="A clear path from discovery to results." body="Switch between the talent and sponsor journey."/><div className="inline-flex w-fit rounded-full border border-[#d8d8d2] bg-white p-1" role="tablist"><button role="tab" aria-selected={mode==="talent"} onClick={()=>setMode("talent")} className={mode==="talent"?"public-pill-active":"public-pill"}>For Talent</button><button role="tab" aria-selected={mode==="sponsor"} onClick={()=>setMode("sponsor")} className={mode==="sponsor"?"public-pill-active":"public-pill"}>For Sponsors</button></div></div><div className="mt-10 grid gap-5 lg:grid-cols-3">{steps.map(([title,body],i)=><article key={title} className="min-h-[230px] rounded-[12px] border border-[#dfdfda] bg-white p-7"><span className="text-sm font-bold text-[#8a7900]">0{i+1}</span><h3 className="mt-8 text-xl font-semibold">{title}</h3><p className="mt-3 leading-7 text-[#606060]">{body}</p></article>)}</div></div></section>}
const initial:CalculatorInput={type:"paid",entryFee:25,participants:100,creatorContribution:0,sponsorContribution:0,adminContribution:0,winners:1,currency:"USD"};
function Calculator({authenticated}:{authenticated:boolean}){const [form,setForm]=useState(initial),r=useMemo(()=>calculateChallengeEconomics(form),[form]),money=(v:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}).format(v);const field=(label:string,key:keyof CalculatorInput)=><label className="grid gap-2 text-sm font-bold">{label}<input type="number" min="0" value={String(form[key])} onChange={e=>setForm(v=>({...v,[key]:Number(e.target.value)}))} className="public-input"/></label>;const rows=[["Gross projected entry revenue",r.grossEntryRevenue],["Winner allocation (65%)",r.generatedWinnerAllocation],["Creator share (20%)",r.creatorShare],["Challenge Suite share (15%)",r.platformShare],["Creator-funded contribution",r.creatorContribution],["Sponsor-funded contribution",r.sponsorContribution],["Admin promotional funding",r.adminContribution]];return <section className="public-section"><div className="public-container"><div className="overflow-hidden rounded-[20px] bg-[#121212] text-white"><div className="grid lg:grid-cols-2"><div className="p-7 sm:p-10 lg:p-12"><p className="text-sm font-bold text-[#f8df35]">Challenge economics calculator</p><h2 className="mt-4 text-3xl font-semibold sm:text-4xl">Plan a challenge with the split in view.</h2><p className="mt-4 leading-7 text-white/65">Estimate potential entry revenue and prize allocation. Projections are not guaranteed revenue.</p><div className="mt-8 grid gap-5 sm:grid-cols-2"><label className="grid gap-2 text-sm font-bold sm:col-span-2">Competition type<select className="public-input" value={form.type} onChange={e=>setForm(v=>({...v,type:e.target.value as CalculatorInput["type"]}))}><option value="paid">Paid-entry challenge</option><option value="free">Free challenge</option><option value="sponsored">Sponsored challenge</option><option value="tournament">Tournament</option><option value="live_event">Live event</option></select></label>{field("Entry fee (USD)","entryFee")}{field("Expected participants","participants")}{field("Creator contribution","creatorContribution")}{field("Confirmed sponsor contribution","sponsorContribution")}{field("Admin promotional contribution","adminContribution")}{field("Number of winners","winners")}</div><Link href={calculatorCreationHref(form,authenticated)} onClick={()=>trackPublicEvent("calculator_completed",{type:form.type,gross:r.grossEntryRevenue})} className="public-primary-button mt-8 w-full justify-center">Start building<ArrowRight size={17}/></Link></div><div className="bg-[radial-gradient(circle_at_75%_20%,rgba(248,223,53,.22),transparent_35%),#1b1b1b] p-7 sm:p-10 lg:p-12"><p className="text-sm font-bold text-white/60">Projected estimate</p><p className="mt-3 text-4xl font-semibold text-[#f8df35]">{money(r.projectedPrizeValue)}</p><p className="mt-2 text-sm text-white/55">Projected prize value</p><div className="mt-8">{rows.map(([label,value])=><div key={String(label)} className="flex justify-between gap-4 border-b border-white/10 py-3 text-sm"><span className="text-white/65">{label}</span><b>{money(Number(value))}</b></div>)}</div><p className="mt-6 text-xs leading-5 text-white/50">{r.processingFeeNote}</p></div></div></div></div></section>}
function Pricing({plans,authenticated}:{plans:Plan[];authenticated:boolean}){const [annual,setAnnual]=useState(false),fallback:Plan[]=[{id:"creator",name:"Creator",priceMonthly:24.99,features:["Creator Studio dashboard","3 challenges/month + 1 private challenge/month","Sponsor-enabled challenge tools","Basic creator analytics","1 Challenge Boost/month"]},{id:"host",name:"Host",priceMonthly:119,features:["Host Control Center","Unlimited public/private challenges","Tournament and live-event tools","Participant, submission, and voting controls","Sponsor proposals, reports, and exports"]}],rows=plans.length?plans:fallback;return <section className="public-section bg-[#f5f5f1]" id="pricing"><div className="public-container"><div className="flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between"><Heading eyebrow="Plans" title="Create consistently. Host professionally." body="Choose the workspace that matches how you run competitions."/><div className="inline-flex w-fit rounded-full border border-[#d8d8d2] bg-white p-1"><button onClick={()=>setAnnual(false)} className={!annual?"public-pill-active":"public-pill"}>Monthly</button><button onClick={()=>setAnnual(true)} className={annual?"public-pill-active":"public-pill"}>Yearly · save 20%</button></div></div><div className="mx-auto mt-10 grid max-w-5xl gap-5 lg:grid-cols-2">{rows.map(plan=>{const host=plan.id==="host",price=annual?(host?1142.4:239.9):(plan.priceMonthly||0),href=plan.current?"/subscriptions":protectedPublicHref("/subscriptions?plan="+plan.id+"&billing="+(annual?"yearly":"monthly"),authenticated);return <article key={plan.id} className={"flex min-h-[560px] flex-col rounded-[14px] border bg-white p-7 sm:p-9 "+(host?"border-[#d3b900] shadow-xl":"border-[#dcdcd7]")}>{host?<span className="w-fit rounded-full bg-[#fff2a3] px-3 py-1 text-xs font-bold">Popular</span>:<span className="h-6"/>}<h3 className="mt-5 text-3xl font-semibold">{plan.name}</h3><p className="mt-3 min-h-14 text-[#626262]">{host?"For professional organisers managing tournaments, private competitions and live events.":"For talents and creators ready to publish competitions consistently and grow their audience."}</p><p className="mt-7 text-4xl font-semibold">{"$"+price.toLocaleString(undefined,{minimumFractionDigits:annual?2:0,maximumFractionDigits:2})}<span className="text-base font-normal text-[#666]">/{annual?"year":"month"}</span></p><ul className="mt-8 grid gap-4">{plan.features.slice(0,7).map(f=><li key={f} className="flex gap-3 text-sm leading-6"><Check size={18} className="mt-1 shrink-0 text-[#9a8600]"/>{f}</li>)}</ul><Link href={href} onClick={()=>trackPublicEvent("plan_selected",{plan:plan.id,billing:annual?"yearly":"monthly"})} className={(host?"public-primary-button":"public-secondary-button")+" mt-auto justify-center"}>{plan.current?"Current plan":host?"Start hosting":"Start creating"}</Link></article>})}</div></div></section>}
function Stories({items}:{items:Data["stories"]}){if(!items.length)return null;return <section className="public-section"><div className="public-container"><Heading eyebrow="Real stories" title="What people are building through Challenge Suite." body="Published stories are reviewed before they appear here."/><div className="mt-10 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{items.slice(0,6).map(s=><blockquote key={s.id} className="rounded-[12px] border border-[#deded8] p-7"><p className="leading-7 text-[#444]">“{s.quote}”</p><footer className="mt-7"><b>{s.displayName}</b><small className="ml-2 text-[#777]">{s.role}</small></footer></blockquote>)}</div></div></section>}
function Assurances({items}:{items:string[]}){const icons=[Trophy,ShieldCheck,UsersRound,CircleDollarSign,Sparkles,Check];return <section className="border-y border-[#deded8] py-10"><div className="public-container grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{items.map((x,i)=>{const Icon=icons[i%icons.length];return <div key={x} className="flex items-center gap-3 text-sm font-bold"><Icon size={19} className="text-[#917e00]"/>{x}</div>})}</div></section>}
function Final({content,authenticated,sponsor}:{content:typeof DEFAULT_PUBLIC_CONTENT.finalCta;authenticated:boolean;sponsor:boolean}){const href=sponsor?"/sponsor/dashboard":authenticated?"/explore":"/auth/register?intent=talent&next=%2Fexplore";return <section className="public-section"><div className="public-container"><div className="rounded-[20px] bg-[linear-gradient(120deg,#f7df32,#ffe988_55%,#fff2b7)] px-7 py-14 sm:px-12 lg:px-16 lg:py-20"><h2 className="max-w-3xl text-4xl font-semibold sm:text-5xl">{sponsor?"Find a competition worth supporting.":content.headline}</h2><p className="mt-5 max-w-2xl text-lg leading-8 text-black/65">{sponsor?"Discover sponsor-ready challenges and campaign opportunities built around real communities.":content.body}</p><Link href={href} className="mt-8 inline-flex min-h-12 items-center gap-2 rounded-[8px] bg-[#171717] px-6 font-bold text-white">{sponsor?"Explore sponsorships":content.label}<ArrowRight size={17}/></Link></div></div></section>}
function Heading({eyebrow,title,body}:{eyebrow:string;title:string;body:string}){return <div className="max-w-3xl"><p className="text-sm font-bold text-[#8b7900]">{eyebrow}</p><h2 className="mt-4 text-[clamp(2rem,4vw,3rem)] font-semibold leading-tight">{title}</h2><p className="mt-4 max-w-2xl text-base leading-7 text-[#636363] sm:text-lg">{body}</p></div>}
