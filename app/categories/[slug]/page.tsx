import type {Metadata} from "next";import {notFound} from "next/navigation";import {CategoryExperience} from "@/components/public-site/category-experience";import {categoryBySlug,PUBLIC_CATEGORIES} from "@/lib/public-site/config";
export function generateStaticParams(){return PUBLIC_CATEGORIES.map(x=>({slug:x.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const category=categoryBySlug((await params).slug);if(!category)return{};return{title:category.name+" challenges",description:category.description,alternates:{canonical:"/categories/"+category.slug},openGraph:{title:category.name+" challenges | Challenge Suite",description:category.description,url:"/categories/"+category.slug}}}
export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const category=categoryBySlug((await params).slug);if(!category)notFound();const {icon:_,...publicCategory}=category;const jsonLd={"@context":"https://schema.org","@type":"CollectionPage",name:category.name+" challenges",description:category.description,url:"https://www.challengesuite.com/categories/"+category.slug};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><CategoryExperience category={publicCategory}/></>}
