import { getAdminDb } from "@/lib/firebase/admin";
import { requireRequestUser } from "@/lib/server/auth";
import { buildRewardSummary, executeRewardSpin, type RewardSpinTier } from "@/lib/server/rewards";
import { fail, ok, readJson, serverError, serverUnavailable } from "@/lib/server/responses";
export const dynamic = "force-dynamic";
export async function GET(request: Request) { const { user, response } = await requireRequestUser(request); if (response) return response; const db = getAdminDb(); if (!db) return serverUnavailable("Rewards"); return ok(await buildRewardSummary(db, user.uid), "Rewards loaded."); }
export async function POST(request: Request) { const { user, response } = await requireRequestUser(request); if (response) return response; const db = getAdminDb(); if (!db) return serverUnavailable("Rewards"); const parsed = await readJson(request); if (parsed.response) return parsed.response; const tier = String(parsed.body?.tier ?? "basic") as RewardSpinTier; if (!["basic", "standard", "premium"].includes(tier)) return fail("Select a valid Spin tier.", 400, undefined, "INVALID_REWARD_TIER"); try { return ok(await executeRewardSpin(db, { userId: user.uid, tier, idempotencyKey: String(parsed.body?.idempotencyKey ?? "").slice(0, 140) || null, paymentSource: parsed.body?.paymentSource === "bonus_spin" ? "bonus_spin" : "points", bonusEntitlementId: typeof parsed.body?.bonusEntitlementId === "string" ? parsed.body.bonusEntitlementId : null }), "Spin confirmed. Your reward was selected securely."); } catch (error) { const code = error instanceof Error ? error.message : "SPIN_FAILED"; const messages: Record<string, string> = { INSUFFICIENT_REWARD_POINTS: "You do not have enough Reward Points for this Spin.", BONUS_SPIN_UNAVAILABLE: "This Bonus Spin is no longer available.", BONUS_SPIN_EXPIRED: "This Bonus Spin has expired.", NO_AVAILABLE_PRIZES: "No eligible rewards are currently available for this tier.", NO_ACTIVE_WHEEL_VERSION: "This Spin tier is temporarily unavailable. No points were charged.", WHEEL_VERSION_CHANGED: "This Spin tier was just updated. No points were charged; please try again.", WHEEL_DISABLED: "This Spin tier is paused right now.", REWARDS_DISABLED: "Rewards are unavailable right now.", REWARD_FULFILLMENT_PAUSED: "Spins are paused while reward fulfillment is under maintenance.", REWARDS_ACCOUNT_RESTRICTED: "Your Rewards account is restricted. You can still view your history.", REWARDS_ACCOUNT_NOT_ELIGIBLE: "This account context is not eligible for consumer Rewards.", REWARD_DEBT_ACTIVE: "New Spins are unavailable while a Reward Point correction is outstanding.", REWARD_POINT_RETURN_RATIO_BLOCKED: "This Spin configuration is paused for economic review." }; return code in messages ? fail(messages[code], 409, undefined, code) : serverError("We could not complete this Spin. No Reward Points were charged.", code); } }
